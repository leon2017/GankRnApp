import { Dimensions } from 'react-native';

const colors = {
  colorPrimary: '#FF3D00',
  colorPrimaryDark: '#DD2C00',
  colorAccent: '#FF9E80',
  tintColor: '#00ADB5',
  textColor: '#00ADB5',
  white: '#fff',
  black: '#000000',
  backgroundColor: '#FAFAFA',
  blackAlpha50: '#80000000'
};

const window = Dimensions.get('window');

export { colors, window };
